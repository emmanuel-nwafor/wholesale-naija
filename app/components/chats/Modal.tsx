import React from 'react'

export default function Modal() {
  return (
    <div>
      This is the two way modal that displays in the chat page. When the three dot icon is clicked. When on mobile and 
      small screens, it is more like a mobile to the top, but when on large screens it is more like a popup.
    </div>
  )
}
