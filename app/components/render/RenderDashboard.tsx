import React from 'react'

export default function RenderDashboard() {
  return (
    <div>
      This page renders the dashboard to the /store/dashboard. It compiles the sidebar, analytics etc.
    </div>
  )
}
